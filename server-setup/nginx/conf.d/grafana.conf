server {
    listen 80;
    listen [::]:80;
    server_name grafana.yatribhavan.umiyadhams.com;

    location / {
        proxy_pass http://hospitia-grafana:3000;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host  $host;
        proxy_set_header X-Forwarded-Port  $server_port;
    }
}

# HTTPS block (enable after SSL certificates are available)
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name grafana.yatribhavan.umiyadhams.com;
#
#     ssl_certificate     /etc/nginx/ssl/grafana/certificate.crt;
#     ssl_certificate_key /etc/nginx/ssl/grafana/private.key;
#
#     location ^~ /.well-known/pki-validation/ {
#         alias /var/www/cert-validation/.well-known/pki-validation/;
#     }
#
#     location / {
#         proxy_pass http://hospitia-grafana:3000;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host  $host;
#         proxy_set_header X-Forwarded-Port  $server_port;
#     }
# }